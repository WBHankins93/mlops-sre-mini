apiVersion: v1
kind: Service
metadata:
  name: {{ include "iris-api.fullname" . }}
  labels:
    {{- include "iris-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: {{ include "iris-api.name" . }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
